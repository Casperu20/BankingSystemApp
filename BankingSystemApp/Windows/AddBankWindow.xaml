<Window x:Class="BankingSystemApp.AddBankWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Bank" Height="420" Width="400"
        WindowStartupLocation="CenterScreen" Background="#FFFDFDFD">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🏦 Add a New Bank" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,20,0,0">
            <TextBlock Text="Bank Name:" Margin="0,5,0,0"/>
            <TextBox x:Name="NameBox" ToolTip="Enter the bank name" />

            <TextBlock Text="SWIFT Code:" Margin="0,10,0,0"/>
            <TextBox x:Name="SwiftBox" ToolTip="Enter SWIFT code" />

            <TextBlock Text="Country:" Margin="0,10,0,0"/>
            <ComboBox x:Name="CountryBox" />

            <TextBlock Text="Location:" Margin="0,10,0,0"/>
            <ComboBox x:Name="LocationBox" />
        </StackPanel>

        <Button Grid.Row="5" Content="➕ Add Bank" 
                Click="AddBank_Click"
                Height="40" Margin="0,20,0,0"
                Background="#FF2B579A" Foreground="White" FontWeight="Bold"/>
    </Grid>
</Window>